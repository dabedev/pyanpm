# TODO

- During install, prompt for `Global Config` selection when no config path has been chosen yet.
- Review whether first-run setup should combine `Global Config` and `Roblox plugins directory` selection in a single onboarding flow.
